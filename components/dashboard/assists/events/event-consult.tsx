import {
  Card,
  CardContent,
  CardDescription,
  CardHeader,
  CardTitle,
} from "@/components/ui/card";

const EventConsult = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Consultar eventos</CardTitle>
        <CardDescription>
          Añade nuevos eventos con sus datos correspondientes
        </CardDescription>
      </CardHeader>
      <CardContent></CardContent>
    </Card>
  );
};

export default EventConsult;
